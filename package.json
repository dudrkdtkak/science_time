{
  "name": "science_time",
  "version": "1.0.0",
  "description": "매일 10분 과학 웹앱",
  "main": "index.js",
  "scripts": {
    "start": "live-server public",
    "build": "echo \"No build step required\"",
    "commit": "git add public && git commit -am \"Auto commit\" && git push",
    "watch": "chokidar \"public/**/*\" -c \"npm run commit\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "송영강",
  "license": "ISC",
  "devDependencies": {
    "chokidar-cli": "^3.0.0",
    "live-server": "^1.2.1"
  }
}
